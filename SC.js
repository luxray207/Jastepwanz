function hapusEmail() {
    if (confirm("Apakah Anda yakin ingin menghapus semua email?")) {
        alert("Email berhasil dihapus!");
        // Tambahkan aksi tambahan jika diperlukan, seperti menghapus data dari server
    }
}
